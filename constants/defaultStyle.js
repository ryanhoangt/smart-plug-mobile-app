import { StyleSheet } from 'react-native'

export const defaultStyles = StyleSheet.create({
  container: {
    marginTop: 12,
    paddingHorizontal: 12
  },

  sectionTitle: {
    fontSize: 18,
    fontWeight: "bold"
  }
})
